import React from 'react';
import NavBar from './NavBar';

export default function Header () {
  return (
    /* Navigation start */
    (
      <header>
        <NavBar />
      </header>
    )
    /* Navigation end  */
  );
}
